import { createTestGroupMetadata } from "../../../_utils/Metadata.js";

export default new createTestGroupMetadata({
	prefix: "grader_test",
	indexStart: 1,
	indexEnd: 50,
	inputExtension: "in",
	outputExtension: "ok",
});
